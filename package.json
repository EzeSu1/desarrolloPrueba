{
  "name": "root-project",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"BACK,FRONT\" --prefix-colors \"bgBlue.bold,bgGreen.bold\" \"npm run dev --prefix backend\" \"npm start --prefix Frontend/client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cypress": "^15.6.0"
  }
}
