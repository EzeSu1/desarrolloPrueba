TODO: 1) En todos los controlers hay algo de la forma:
            if (result_id.error) {
                res.status(400).json(result_id.error.issues)
                return
            }
            const id = result_id.data
            const usuario = this.usuariosService.obtenerUsuario(id);
            if (!usuario) {
                res.status(404).json({error: "Usuario no encontrado"})
                return
            }
         Al menos la primera parte la podemos evitar haciendo una función que haga la validacion.


TODO: 2) Getters y setter

TODO: 3) Implementar los DTOs para todas las entidades (Directorio DTO)

TODO: 4) Cambiar el nombre del Patch de pedidos (en realiadd es actualizarEstadoPedido)

TODO: 5) Corregir tema devoluciones: hay veces que la request tira 500 cuando no ha sucedido errores ciertamente.
         Pasa con los Throw new Error()